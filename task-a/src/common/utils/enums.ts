export enum ResourceStatusCode {
  OK = 200,
  NoContent = 204,
  BadRequest = 400,
  NotFound = 404,
  InternalServerError = 500,
  NotImplemented = 501,
}

export enum GenderEnum {
  male = 'male',
  female = 'female',
}
